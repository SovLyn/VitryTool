# 安全政策

## 报告方式

发现安全漏洞请**优先直接在仓库提交 issue**，并打上 `security` 标签。提交时请说明：

- 漏洞类型与影响范围
- 复现步骤（尽量最小化）
- 受影响版本
- （如有）修复建议

## 处理承诺

维护者会尽快确认并处理 issue。修复会在下一个版本中发布，并记录于 `CHANGELOG.md`。

## 项目说明

VitryTool 目前为**纯本地运行**的桌面应用，无遥测、无数据上报、不访问云端服务。即便如此，局域网通信类功能的网络安全边界（如数据加密、访问控制）是关注重点，相关设计会写入功能文档。
